
name: Zephyr

# https://docs.github.com/en/actions/reference/workflow-syntax-for-github-actions#onschedule
on: [push]

jobs:
  zephy_daily:
    runs-on: ubuntu-latest

    name: Zephyr Continuous Builds
    steps:
      - uses: actions/checkout@v2
      - name: Test
        run: |
	  docker run --rm -v `pwd`:/opt/tflite-micro zephyrprojectrtos/ci:latest /opt/tflite-micro/tensorflow/lite/micro/tools/ci_build/test_zephyr.sh
